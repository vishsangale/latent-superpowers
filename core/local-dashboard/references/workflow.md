# Workflow

1. Point the dashboard at local MLflow and W&B sources.
2. Build a normalized run index first.
3. Serve simple JSON APIs and one static HTML view.
4. Prefer grouped comparisons over raw run dumps when answering research questions.
5. Keep artifact browsing local and read-only.
